<?xml version="1.0" encoding="utf-8"?>
<layer-list
  xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="@dimen/space_02" />
                <gradient android:startColor="@color/them_color" android:endColor="@color/them_color" android:centerColor="@color/them_color" />
            </shape>
        </clip>
    </item>
</layer-list>